apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: file-service
spec:
  rules:
    - host: ${FILE_SERVICE_HOST}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: file-service
                port:
                  number: 8085

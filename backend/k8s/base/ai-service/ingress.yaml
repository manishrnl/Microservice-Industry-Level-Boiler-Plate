apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-service
spec:
  rules:
    - host: ${AI_SERVICE_HOST}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ai-service
                port:
                  number: 8086
